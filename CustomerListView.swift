import SwiftUI

struct CustomerListView: View {
    var body: some View {
        NavigationView {
            Text("Customer List Page")
                .navigationBarTitle("Customer List")
        }
    }
}
